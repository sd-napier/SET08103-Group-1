version: '3'
services:
  # Build app from Dockerfile in same folder
  app:
    build: .

  # Build the db as name db from the Dockerfile in the db folder
  # exposing port 33060 so we can run queries from outside the container
  db:
    build: db/.
    ports:
      - "33060:3306"
